This is a simple PHP testing suite that was initially a part of the
Student Robotics IDE (cyanide), but which has since been broken out.

You should put a list of tests in test-schedule, and then run the tests
by calling run-tests
